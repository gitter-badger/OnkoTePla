<controls:MetroWindow x:Class="bytePassion.OnkoTePla.Client.WpfUi.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
					  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"                     
                      xmlns:mainWindow="clr-namespace:bytePassion.OnkoTePla.Client.WpfUi.ViewModels.MainWindow"                      
                      xmlns:views="clr-namespace:bytePassion.OnkoTePla.Client.WpfUi.Views"
                      xmlns:converter="clr-namespace:bytePassion.Lib.WpfLib.Converter;assembly=bytePassion.Lib"
                      xmlns:local="clr-namespace:bytePassion.OnkoTePla.Client.WpfUi"
                      Title="OnkoTePla"
                      MinWidth="700"
                      MinHeight="500"
                      d:DesignHeight="500"
                      d:DesignWidth="700"
                      mc:Ignorable="d">
    
    <d:UserControl.DataContext>
        <mainWindow:MainWindowViewModelSampleData/>
    </d:UserControl.DataContext>

    <controls:MetroWindow.Resources>
        <converter:BoolToCollapsedVisibilityConverter x:Key="BoolToCollapsedVisibilityConverter"/>
    </controls:MetroWindow.Resources>

    <Grid>

        <views:MainView x:Name="MainView" 
                        DataContext="{Binding MainViewModel}"
                        Visibility="{Binding DataContext.IsMainViewVisible,
                                             Converter={StaticResource BoolToCollapsedVisibilityConverter},
                                             RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}"/>

        <views:NotificationServiceContainer Width="700"										  
										    HorizontalAlignment="Center"
										    VerticalAlignment="Top"
											DataContext="{Binding NotificationServiceContainerViewModel}"/>

    </Grid>
</controls:MetroWindow>