<controls:MetroWindow x:Class="bytePassion.OnkoTePla.Client.WPFVisualization.Views.AddAppointmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:addAppointmentDialog="clr-namespace:bytePassion.OnkoTePla.Client.WPFVisualization.ViewModels.AddAppointmentDialog"
        mc:Ignorable="d"
		WindowStyle="ToolWindow"		
		WindowStartupLocation="CenterOwner"
        Title="AddAppointmentDialog" Height="600" Width="800">
	
	<d:DesignData.DataContext>
        <addAppointmentDialog:AddAppointmentDialogViewModelSampleData/>
	</d:DesignData.DataContext>
	
    <Grid Background="Bisque">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0"
                Grid.Row="0"
                Grid.RowSpan="3"
                Margin="5"
                Background="Blue"/>

        <Border Grid.Column="1"
                Grid.Row="0"
                Margin="5"
                Background="Cyan"/>

        <Border Grid.Column="1"
                Grid.Row="1"
                Margin="5"
                Background="Teal"/>

        <Border Grid.Column="1"
                Grid.Row="2"
                Margin="5"
                Background="Cyan"/>
		
		<Button Grid.Row="3"
				Grid.Column="1"
				Margin="20"
				Content="Close"
				Command="{Binding CloseDialog}"></Button>

    </Grid>
</controls:MetroWindow>
